*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/
.worktrees/

# Release signing — never commit
keystore.properties
*.jks
*.keystore

# Local tooling / agent scratch files
/.idea/
/.codex
/.claude.md
/prompt.md

# Build output. The signed bundle is 30 MB and is regenerated by
# `./gradlew bundlePlayRelease` — it has no business in the history.
/app/play/
/app/fdroid/
*.aab
*.apk

# A screenshot that was saved with no name in front of the extension.
/.png

# Personal notes about how to talk about this project. Not the repository's
# business, and it reads badly to the people it is written for.
/docs/INTERVIEW_PREP.md
