# Archive

This directory contains documentation from the pre-redesign (v1) architecture of Voyager. These files are kept for historical reference only.

For current architecture documentation, see `blueprint.md` in the project root and the `docs/architecture/` directory.
